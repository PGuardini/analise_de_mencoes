[project]
name = "analise-de-mencoes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pedro Guardini", email = "pgabiattiguardini@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "python-dateutil>=2.9.0.post0",
    "sqlmodel>=0.0.46",
]

[project.scripts]
analise-de-mencoes = "analise_de_mencoes:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
